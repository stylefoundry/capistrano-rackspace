require 'spec_helper'

describe Capistrano::Rackspace do
  it 'has a version number' do
    expect(Capistrano::Rackspace::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
